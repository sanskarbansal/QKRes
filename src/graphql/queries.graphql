query loadPosts {
    getPosts {
        id
        title
        user {
            username
            email
            id
        }
        date
    }
}
query ME {
    Me {
        username
        id
        email
    }
}
